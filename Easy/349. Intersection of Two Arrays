class Solution:
    def intersection(self, nums1: List[int], nums2: List[int]) -> List[int]:
        result=set()
        set1=set()
        for num in nums1:
            set1.add(num)
        for num in nums2:
            if num in set1:
                result.add(num)
        return list(result)
